#!/usr/bin/env lua
require("luarocks.loader")

local Shell = require("minilib.shell")

return {
	configure = function()
		Shell.ln("$(pwd)/foot/my", "~/.config/foot")
	end,
	dependsOn = function()
		return Shell.file_exists("foot")
	end,
	installer = function() end,
}
