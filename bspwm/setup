#!/usr/bin/env lua
require("luarocks.loader")

local Shell = require("minilib.shell")

return {
	depends = {
		"bspwm",
		"sxhkd",
		"feh",
		"lxpolkit",
		"tint2",
		"picom",
		"dunst",
		"lemonbar",
		["ictl"] = { method = "github/bucketsize" },
		["m360"] = { method = "github/bucketsize" },
	},
	configure = function()
		Shell.ln("$(pwd)/bspwm/my", "~/.config/bspwm")
	end,
	install = function() end,
}
